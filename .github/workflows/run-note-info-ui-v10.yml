name: Run Note Info UI v10 patch

on:
  push:
    branches:
      - dev
    paths:
      - .github/run-note-info-ui-v10

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: dev
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: python .github/scripts/note_info_ui_v10.py
      - run: node --check main.js
      - run: |
          git config user.name "H4d33r"
          git config user.email "79756288+h4d33r@users.noreply.github.com"
          git add main.js styles.css manifest.json versions.json
          git diff --cached --check
          git commit -m "feat: improve note info and workspace header"
          git push origin dev
