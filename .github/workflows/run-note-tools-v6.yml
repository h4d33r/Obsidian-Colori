name: Run Note Tools v6 patch

on:
  push:
    branches:
      - dev
    paths:
      - .github/run-note-tools-v6

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: dev
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: python .github/scripts/note_tools_v6.py
      - run: node --check main.js
      - run: |
          git config user.name "H4d33r"
          git config user.email "79756288+h4d33r@users.noreply.github.com"
          git add main.js styles.css
          git diff --cached --check
          git commit -m "feat: improve IOC groups and related note connections"
          git push origin dev
